apiVersion: v1
kind: Service
metadata:
  name: my-tech-app-service  
spec:
  type: LoadBalancer 
  selector:
    app: my-tech-app      
  ports:
  - protocol: TCP
    port: 80              
    targetPort: 5000 
